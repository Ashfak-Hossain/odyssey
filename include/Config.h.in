#ifndef CONFIG_H
#define CONFIG_H

#define PLATFORM_@PLATFORM_NAME@

#if defined(PLATFORM_MACOS)
    #define PLATFORM_MACOS 1
#elif defined(PLATFORM_WINDOWS)
    #define PLATFORM_WINDOWS 1
#endif

#endif